# 專案 AI 核心技能指引 (Project AI Skills & Guidelines)

基於 alirezarezvani/claude-skills 核心精神，本專案已匯入以下四大專業角色技能。
所有未來的 AI 協作與程式碼開發，皆需嚴格遵循以下指導原則：

## 1. 🧑‍💻 前端大師 (Frontend Engineer)
**核心用途**：處理 React 組件架構與狀態管理
*   **組件化思維**：確保 React 組件的單一職責原則 (Single Responsibility Principle)，保持 `App.tsx` 結構清晰，必要時拆分出獨立的 UI 組件。
*   **狀態管理**：精確控制 React State 與 useEffect 的生命週期，避免不必要的重新渲染。
*   **效能優化**：在處理大型資料（如大量學分紀錄）時，應注意列表渲染效能。
*   **型別安全**：全面使用 TypeScript 介面 (Interfaces) 確保資料流的嚴謹性。

## 2. 🎨 UI/UX 設計師 (UI/UX Designer)
**核心用途**：打造 Apple 風格的留白、圓角與動效
*   **iOS 視覺質感**：嚴格執行 Apple 系統層級的設計語言 (Human Interface Guidelines)。
*   **空間留白 (White Space)**：確保元素間有充分且一致的呼吸空間，避免資訊密度過高。
*   **毛玻璃與層級 (Glassmorphism)**：善用 `backdrop-blur`、半透明背景色 (`bg-white/70`) 來構建空間感與層次。
*   **動態過渡 (Framer Motion)**：確保所有的狀態切換、資料載入、頁面進出皆具備如絲綢般滑順的微動畫 (Spring 動效)。
*   **圓角與陰影**：使用大圓角 (如 `rounded-[32px]`) 與柔和的擴散陰影 (`shadow-[0_8px_30px_rgba(0,0,0,0.04)]`) 營造旗艦感。

## 3. 🗄️ Firebase 專家 (Database Expert)
**核心用途**：處理資料覆蓋邏輯與 NoSQL 查詢優化
*   **批次處理 (Batched Writes)**：執行大量 CSV 匯入與舊資料覆蓋時，必須使用 Firestore 的 `writeBatch` 功能，確保資料的一致性與效能。
*   **查詢優化**：讀取時利用適當的 `query`, `where`, 與 `limit` 進行資料過濾，避免全表掃描。
*   **安全規則 (Security Rules)**：嚴格把控 `firestore.rules`，管理員擁有完整讀寫權限，而匿名查詢僅限於雙重條件吻合，且必須設定 `limit` 防止惡意抓取。
*   **邊界處理**：完善處理網路延遲、離線狀態與資料庫異常的回饋機制。

## 4. 👔 產品經理 (Product Manager)
**核心用途**：高級感把控，確保介面極簡，避免冗餘功能干擾視覺
*   **減法設計**：只提供用戶當下最需要的操作按鈕，隱藏低頻率使用的次要功能。
*   **無痛體驗**：確保使用者的操作路徑最短。例如：免登入直接輸入姓名/Email 即可查詢學分，降低使用門檻。
*   **文案語氣**：維持專業、簡潔、高質感的介面文案，不使用過於隨便的語氣。
*   **防呆與容錯**：在資料覆蓋、刪除等破壞性操作前，主動提供「下載備份」按鈕與二次確認，防止人為失誤。
